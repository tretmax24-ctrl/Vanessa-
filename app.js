const sb=window.supabaseClient,$=s=>document.querySelector(s),app=$('#app'),status=$('#status');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function authView(){app.innerHTML='<section class="card auth-card"><h1>Welcome to MAX</h1><p class="muted">Connect, chat, call and use AI.</p><div class="tabs"><button id="loginTab">Sign in</button><button id="signupTab" class="secondary">Create account</button></div><form id="authForm"><div id="signupFields" hidden><label>Username</label><input id="username" minlength="3" maxlength="30"><label>Display name</label><input id="displayName" maxlength="80"></div><label>Email</label><input id="email" type="email" required><label>Password</label><input id="password" type="password" minlength="6" required><button id="submitAuth">Sign in</button></form><p id="formMessage" class="muted"></p></section>';let mode='login';$('#loginTab').onclick=()=>{mode='login';$('#signupFields').hidden=true;$('#submitAuth').textContent='Sign in'};$('#signupTab').onclick=()=>{mode='signup';$('#signupFields').hidden=false;$('#submitAuth').textContent='Create account'};$('#authForm').onsubmit=async e=>{e.preventDefault();const m=$('#formMessage');m.textContent='Working...';try{if(mode==='login'){const r=await sb.auth.signInWithPassword({email:$('#email').value.trim(),password:$('#password').value});if(r.error)throw r.error}else{const r=await sb.auth.signUp({email:$('#email').value.trim(),password:$('#password').value,options:{data:{display_name:$('#displayName').value.trim()}}});if(r.error)throw r.error;if(!r.data.session){m.textContent='Account created. Check your email to confirm, then sign in.';return}const p=await sb.from('profiles').upsert({id:r.data.user.id,username:$('#username').value.trim().toLowerCase(),display_name:$('#displayName').value.trim()});if(p.error)throw p.error}await render()}catch(e){m.textContent=e.message||'Authentication failed'}}}
async function profileView(user){const r=await sb.from('profiles').select('*').eq('id',user.id).single();if(r.error){status.textContent=r.error.message;return}const p=r.data;app.innerHTML=`<section class="card"><div class="avatar">${esc((p.display_name||'M')[0]).toUpperCase()}</div><h1>${esc(p.display_name)}</h1><p class="muted">@${esc(p.username)}</p><form id="pf"><label>Display name</label><input id="pn" value="${esc(p.display_name)}" required><label>Username</label><input id="pu" value="${esc(p.username)}" required><label>Bio</label><textarea id="pb">${esc(p.bio||'')}</textarea><button>Save profile</button><button type="button" class="secondary" id="out">Sign out</button></form></section>`;$('#pf').onsubmit=async e=>{e.preventDefault();const r=await sb.from('profiles').update({display_name:$('#pn').value.trim(),username:$('#pu').value.trim().toLowerCase(),bio:$('#pb').value.trim()}).eq('id',user.id);if(r.error)status.textContent=r.error.message;else{status.textContent='Profile saved.';profileView(user)}};$('#out').onclick=async()=>{await sb.auth.signOut();render()}}
async function render(){const r=await sb.auth.getUser();if(!r.data.user){$('#topActions').innerHTML='';authView();return}const u=r.data.user;$('#topActions').innerHTML='<button id="profileBtn">My profile</button>';app.innerHTML='<section class="card"><h1>MAX Community</h1><p class="muted">You are signed in and connected to Supabase.</p><button id="open">Open profile</button></section>';$('#profileBtn').onclick=()=>profileView(u);$('#open').onclick=()=>profileView(u)}
sb.auth.onAuthStateChange(()=>render());render();