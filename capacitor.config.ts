import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.max.social',
  appName: 'MAX',
  webDir: 'www',
  server: {
    url: 'https://tretmax24-ctrl.github.io/Vanessa-/',
    cleartext: false
  }
};

export default config;
