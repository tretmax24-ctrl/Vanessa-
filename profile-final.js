(()=>{'use strict';const sb=window.supabaseClient,$=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));const initial=v=>(String(v||'M').trim().charAt(0).toUpperCase()||'M');
async function load(){const uid=window.__MAX_USER?.id;if(!uid)return null;const r=await sb.from('profiles').select('id,username,display_name,bio,avatar_url').eq('id',uid).maybeSingle();if(r.error||!r.data)return null;window.__MAX_PROFILE=r.data;window.__MAX_AVATAR_URL=r.data.avatar_url||'';const p=$('.profile-page'),a=p?.querySelector('.profile-avatar-large');if(a)a.innerHTML=r.data.avatar_url?`<img src="${esc(r.data.avatar_url)}" alt="Profile photo">`:`<span>${initial(r.data.display_name)}</span>`;return r.data}
async function upload(file){const uid=window.__MAX_USER?.id;if(!uid)throw Error('Please sign in again.');if(!file?.type.startsWith('image/'))throw Error('Choose an image.');if(file.size>8*1024*1024)throw Error('Profile photo must be 8 MB or smaller.');const ext=(file.name.split('.').pop()||'jpg').toLowerCase(),path=`${uid}/profile-avatar-${crypto.randomUUID()}.${ext}`;const u=await sb.storage.from('post-media').upload(path,file,{contentType:file.type,upsert:false});if(u.error)throw u.error;const url=sb.storage.from('post-media').getPublicUrl(path).data.publicUrl;const s=await sb.from('profiles').update({avatar_url:url}).eq('id',uid).select('avatar_url').maybeSingle();if(s.error||s.data?.avatar_url!==url)throw Error('Profile photo could not be saved.');window.__MAX_AVATAR_URL=url;return url}
function close(){document.getElementById('max-profile-editor')?.remove()}
async function open(){const p=await load();if(!p)return;close();const root=document.createElement('div');root.id='max-profile-editor';root.innerHTML=`<div class="mpe-backdrop"></div><section class="mpe-card" role="dialog" aria-modal="true"><header class="mpe-head"><div><span>PROFILE</span><h2>Edit profile</h2><p>Update your MAX identity.</p></div><button type="button" id="mpeClose">×</button></header><div class="mpe-body"><div class="mpe-photo"><div id="mpeAvatar">${p.avatar_url?`<img src="${esc(p.avatar_url)}" alt="Profile photo">`:`<span>${initial(p.display_name)}</span>`}</div><div><strong>Profile picture</strong><small>Shown on your MAX profile.</small><button type="button" id="mpeChange">Change profile picture</button><input id="mpeFile" type="file" accept="image/jpeg,image/png,image/webp" hidden></div></div><label><span>Display name</span><input id="mpeName" maxlength="80" value="${esc(p.display_name||'')}" required></label><label><span>Username</span><input id="mpeUsername" maxlength="30" minlength="3" value="${esc(p.username||'')}" required></label><label class="mpe-bio"><span>Bio <em><i id="mpeCount">${String(p.bio||'').length}</i>/280</em></span><textarea id="mpeBio" maxlength="280" placeholder="Tell people about yourself…">${esc(p.bio||'')}</textarea></label><div id="mpeError"></div></div><footer><button type="button" id="mpeCancel">Cancel</button><button type="button" id="mpeSave">Save changes</button></footer></section>`;document.body.appendChild(root);$('#mpeClose').onclick=close;$('#mpeCancel').onclick=close;$('.mpe-backdrop',root).onclick=close;const file=$('#mpeFile'),change=$('#mpeChange'),avatar=$('#mpeAvatar'),bio=$('#mpeBio'),count=$('#mpeCount'),err=$('#mpeError'),save=$('#mpeSave');change.onclick=()=>file.click();bio.oninput=()=>count.textContent=bio.value.length;file.onchange=async()=>{if(!file.files?.[0])return;change.disabled=true;change.textContent='Saving…';try{const url=await upload(file);avatar.innerHTML=`<img src="${esc(url)}" alt="Profile photo">`;const page=$('.profile-page'),pa=page?.querySelector('.profile-avatar-large');if(pa)pa.innerHTML=`<img src="${esc(url)}" alt="Profile photo">`;change.textContent='Photo saved';setTimeout(()=>change.textContent='Change profile picture',1200)}catch(e){err.textContent=e.message||'Could not save photo.';change.textContent='Change profile picture'}finally{change.disabled=false;file.value=''}};save.onclick=async()=>{err.textContent='';const name=$('#mpeName').value.trim(),username=$('#mpeUsername').value.trim().toLowerCase().replace(/\s+/g,''),bioValue=bio.value.trim();if(!name)return err.textContent='Display name is required.';if(!/^[a-z0-9._-]{3,30}$/.test(username))return err.textContent='Username must be 3–30 characters.';save.disabled=true;save.textContent='Saving…';const r=await sb.from('profiles').update({display_name:name,username,bio:bioValue||null}).eq('id',window.__MAX_USER.id);if(r.error){err.textContent=r.error.message;save.disabled=false;save.textContent='Save changes';return}await load();close()}}
function bind(){ $$('.profile-avatar-action').forEach(x=>x.remove()); const b=$('#editProfile');if(b&&!b.dataset.mpe){b.dataset.mpe='1';b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();open()},true)}load()}const observer=new MutationObserver(()=>setTimeout(bind,60));observer.observe(document.getElementById('app')||document.body,{childList:true,subtree:true});setTimeout(bind,200)})();